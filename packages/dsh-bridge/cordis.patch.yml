# Layer applied when a profile lists this bundle.
# Verified against the live runtime (dsh 0.1.1-rc.2): the row's `name` resolves
# through Node module resolution, so it is the package name, not a source path.
- insert:
    - id: bridge
      name: dsh-bridge
